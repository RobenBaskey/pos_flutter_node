version: "3.9"

services:
  flutter-web:
    container_name: jonosokti-admin
    build:
      context: . # Dockerfile location
      dockerfile: Dockerfile
    ports:
      - "3001:80" # expose container port 3001 to host
    restart: unless-stopped
