import 'package:flutter/material.dart';

class ReservedQuantity extends StatelessWidget {
  const ReservedQuantity({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}